# Production Environment Configuration
# This file contains non-sensitive configuration values
# Sensitive values are configured in Terraform Cloud workspace variables

environment = "production"

database_size = "small"
